---
phase: ad-hoc / no active GSD phase
task: complete
status: uncommitted changes — 3 files modified
last_updated: 2026-04-08T08:40:33.919Z
branch: pdf
---

<current_state>
Two analytics bug fixes completed this session. Changes are staged but not committed.
Build passes clean (verified with `npm run build`).
</current_state>

<completed_work>

- Fix 1: "Spending this month" including credit transactions
  - `src/lib/transaction-flow.ts` — `isExpenseForMetrics` now checks `tx.transaction_type === 'debit'` directly (removed legacy category-inference fallback that was misclassifying credits as debits for rows without `transaction_type`)
  - `isIncomeForMetrics` similarly updated to `tx.transaction_type === 'credit'`
  - Removed now-unused `categoryForAnalytics` import from `transaction-flow.ts`

- Fix 2: "Top category" category aggregation consistency
  - `src/lib/transaction-analytics.ts` — removed redundant `.trim()` in 'income' exclusion guard in `byCategory` loop (already trimmed by `categoryForAnalytics`)
  - `src/lib/spending-insights.ts` — added `if (cat.toLowerCase() === 'income') continue` to `categoryTotalsForMonth` to match `computeAnalytics` behavior; without this, the insights text could surface 'income' as top category for legacy debit rows
</completed_work>

<remaining_work>

- Nothing pending from these two tasks
- Commit the 3 modified files (see next_action)
- `code-map.ts` and `code-map.md` are untracked but pre-existing — not related to this session's work
</remaining_work>

<decisions_made>

- Used `tx.transaction_type === 'debit'` directly in `isExpenseForMetrics` instead of the `isDebitTransaction` helper (which uses a legacy fallback). Reason: the fallback (`isIncomeCategoryLabel` check) could misclassify credit transactions without `transaction_type` as debits if their category wasn't income-like.
- Kept `transactionEntryType` / `isDebitTransaction` / `isCreditTransaction` unchanged — these are used for UI display (transaction row badges, edit modal init) where the legacy fallback is still appropriate.
- Did NOT add 'income' to `EXCLUDED_ANALYTICS_CATEGORY_LABELS` (which would map it to 'Other') — user requirement was to exclude it entirely, not group it under Other.
</decisions_made>

<blockers>
None.
</blockers>

<context>
Root cause of both bugs: `isExpenseForMetrics` used `isDebitTransaction` → `transactionEntryType`, which falls back to category inference for rows missing `transaction_type`. A credit transaction with a non-income category and no `transaction_type` (legacy data) would be classified as debit and counted in spending/top-category.

The `categoryTotalsForMonth` function in `spending-insights.ts` was the secondary inconsistency: `computeAnalytics` excluded 'income' from `byCategory`, but `buildExpenseInsights` didn't apply the same guard, so insights text could surface 'income' as top category.
</context>

<next_action>
Commit the 3 modified files:
  git add src/lib/transaction-flow.ts src/lib/transaction-analytics.ts src/lib/spending-insights.ts
  git commit -m "fix: spending and top-category analytics to use transaction_type directly"
Then continue with remaining work on the `pdf` branch.
</next_action>
